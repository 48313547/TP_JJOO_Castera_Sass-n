@{
    ViewData["Title"] = "Deportes";
}
@foreach(Deporte deporte in ViewBag.ListaDeportes){
    <div>
        <a href='@Url.Action("VerDetalleDeporte", "Home", new{idDeporte=deporte.IdDeporte})'><img src="@deporte.Foto"></a>
        <h2>@deporte.Nombre</h2>
    </div>
}